<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dangle Diaries Store</title>
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* Reset & base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', Arial, sans-serif;
    }
    body {
      background: linear-gradient(to bottom, #ffe5d9, #cce6ff); /* Peach to soft blue */
      color: #333;
      line-height: 1.5;
    }
    a { text-decoration: none; color: inherit; }

    /* Header */
    header {
      background: #fff;
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      position: sticky;
      top: 0;
      z-index: 1000;
      border-bottom-left-radius: 20px;
      border-bottom-right-radius: 20px;
    }
    header h1 {
      color: #c94f7c;
      font-size: 32px;
      font-family: 'Great Vibes', cursive; /* Stylish brand name */
    }
    nav a {
      margin-left: 20px;
      font-weight: 600;
      color: #555;
      transition: color 0.3s;
    }
    nav a:hover { color: #ff7f7f; }

    /* Hero Section */
    .hero {
      padding: 100px 20px;
      text-align: center;
      background: linear-gradient(135deg, #ffebd6, #ffd1dc);
      border-radius: 20px;
      margin: 20px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    }
    .hero h2 {
      font-size: 42px;
      margin-bottom: 15px;
      color: #c94f7c;
    }
    .hero p {
      font-size: 20px;
      margin-bottom: 30px;
      color: #555;
    }
    .hero button {
      padding: 14px 30px;
      font-size: 18px;
      background: #ff7f7f;
      color: white;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      transition: transform 0.3s, background 0.3s;
    }
    .hero button:hover {
      background: #c94f7c;
      transform: scale(1.05);
    }

    /* Products */
    .products {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 25px;
      padding: 50px 40px;
    }
    .product-card {
      background: #fff;
      border-radius: 20px;
      padding: 20px;
      text-align: center;
      box-shadow: 0 8px 20px rgba(0,0,0,0.15);
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .product-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 15px 30px rgba(0,0,0,0.2);
    }
    .product-card img {
      width: 100%;
      height: auto;
      border-radius: 15px;
      margin-bottom: 15px;
    }
    .product-card h3 {
      font-size: 22px;
      margin-bottom: 10px;
      color: #c94f7c;
    }
    .product-card p {
      font-size: 16px;
      margin-bottom: 15px;
      color: #555;
    }
    .product-card button {
      padding: 12px 25px;
      font-size: 16px;
      background: linear-gradient(to right, #ff7f7f, #ffbaba);
      color: white;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      transition: background 0.3s, transform 0.3s;
    }
    .product-card button:hover {
      background: linear-gradient(to right, #c94f7c, #ff7f7f);
      transform: scale(1.05);
    }

    /* Footer */
    footer {
      background: #333;
      color: #fff;
      text-align: center;
      padding: 25px 40px;
      margin-top: 50px;
    }
    footer a { color: #ff7f7f; }

    /* Responsive */
    @media (max-width: 768px) {
      header { flex-direction: column; }
      nav { margin-top: 10px; }
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <h1>Dangle Diaries</h1>
    <nav>
      <a href="#home">Home</a>
      <a href="#products">Products</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="hero" id="home">
    <h2>Stylish Jewellery for Every Occasion</h2>
    <p>Premium designs that add sparkle to your everyday ✨</p>
    <button onclick="window.location.href='#products'">Shop Now</button>
  </section>

  <!-- Products Section -->
  <section class="products" id="products">
    <div class="product-card">
      <img src="https://via.placeholder.com/250x250.png?text=Earrings" alt="Earrings">
      <h3>Earrings</h3>
      <p>Elegant everyday earrings</p>
      <button>Buy Now</button>
    </div>
    <div class="product-card">
      <img src="https://via.placeholder.com/250x250.png?text=Floral+Earrings" alt="Floral Earrings">
      <h3>Floral Earrings</h3>
      <p>Beautiful floral designs</p>
      <button>Buy Now</button>
    </div>
    <div class="product-card">
      <img src="https://via.placeholder.com/250x250.png?text=Anti-Tarnish+Earrings" alt="Anti-tarnish Earrings">
      <h3>Anti-Tarnish Earrings</h3>
      <p>Long-lasting shine</p>
      <button>Buy Now</button>
    </div>
    <div class="product-card">
      <img src="https://via.placeholder.com/250x250.png?text=Bracelet" alt="Bracelet">
      <h3>Bracelet</h3>
      <p>Perfect accessory for every look</p>
      <button>Buy Now</button>
    </div>
  </section>

  <!-- Footer -->
  <footer id="contact">
    <p>&copy; 2026 Dangle Diaries Store | Made with ❤️ by Mahek</p>
    <p>Follow us: <a href="#">Instagram</a> | <a href="#">WhatsApp</a></p>
  </footer>

</body>
</html>
